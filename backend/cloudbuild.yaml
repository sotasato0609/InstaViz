steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.prod'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/instaviz/backend:latest'
      - '.'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/instaviz/backend:latest'
